// Shared localStorage key for the Finance Management sidebar "unread"
// badge (components/portal/PortalShell.tsx) and the page that clears it
// (app/(portal)/finance/page.tsx). Same pattern as warrantySeen.ts's
// WARRANTY_LAST_SEEN_KEY.
export const FINANCE_LAST_SEEN_KEY = "finance:lastSeenAt";
